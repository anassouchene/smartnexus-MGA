import { Box, Typography } from '@mui/material';
import React from 'react';

const Claims: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Sinistres</Typography>
      <Typography variant="body1">Traiter et suivre les sinistres.</Typography>
    </Box>
  );
};

export default Claims;
